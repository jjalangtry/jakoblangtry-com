{
  "name": "jakoblangtry-com",
  "version": "1.0.0",
  "description": "Jakob Langtry's personal website",
  "scripts": {
    "dev": "astro dev",
    "predev": "node scripts/generate-env.js",
    "build": "astro build",
    "prebuild": "node scripts/generate-env.js",
    "preview": "astro preview",
    "start": "serve -s dist -l ${PORT:-4321}"
  },
  "dependencies": {
    "astro": "latest",
    "serve": "^14.2.5"
  }
}